/*
=====================================
Assignment: Return the First Element in an Array
=====================================

Create a function that takes an array containing only numbers 
and returns the first element.

Function Signature:
function getFirstValue(arr): number

Examples:
getFirstValue([1, 2, 3]) => 1
getFirstValue([80, 5, 100]) => 80
getFirstValue([-500, 0, 50]) => -500

Hints:
- First, try solving using a traditional for loop by accessing index 0 directly.
- This is a good opportunity to understand how arrays are indexed from 0.

Step-by-Step Algorithm:
1. Access the element at index 0 using arr[0].
2. Return this value.
*/

function getFirstValue(arr) {
    // Your code here
   
}
  
module.exports = getFirstValue;
  