/*
=====================================
Assignment: Pair Management
=====================================

Given two arguments, return an array which contains these two arguments.

Function Signature:
function makePair(a, b): any[]

Examples:
makePair(1, 2) => [1, 2]
makePair(51, 21) => [51, 21]
makePair(512124, 215) => [512124, 215]

Hints:
- Use an array literal to place both arguments inside square brackets.
- This is a basic introduction to constructing arrays manually.

Step-by-Step Algorithm:
1. Accept two arguments: `a` and `b`.
2. Create a new array and insert `a` as the first element and `b` as the second.
3. Return the resulting array.
*/

function makePair(a, b) {
    // Your code here
    
}
  
module.exports = makePair;
  